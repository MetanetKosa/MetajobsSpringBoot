server:
  port: 8082
logging:
  level:
    org.springframework: DEBUG
spring:
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:XE
    username: metajobs
    password: 1234

mybatis:
  mapper-locations: classpath:mapper/resume/*.xml
  type-aliases-package: com.example.metajobs.resume
